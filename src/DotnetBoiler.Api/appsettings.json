{
  "Logging": {
    "LogLevel": {
      "Default": "Information",

      // Reduces framework log noise so your own API logs are easier to read.
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // "*" means this API accepts any Host header. Lock this down for real production apps.
  "AllowedHosts": "*"
}
